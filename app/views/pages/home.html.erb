<br />
<!-- Show sign up button if user isn't signed in -->
<% if !user_signed_in? %>
	<div class="homepage-signup">	
		<h4 class="text-center">Create a Polestar Dealership Account to start listing your vehicles: 
		<%= link_to "Sign Up!", new_user_registration_path(plan: @dealer_plan), class: "btn btn-danger btn-md" %></h4>
	</div>
<% end %>

<div class="container">
	<div class="row">		
		<div class="well inventory-well">
			<h1 class="text-center">Shopping for a new vehicle?</h1>
			<br />
			<p class="about-page">Polestar exists to make your life easier. All overseas military car dealerships can list their vehicles on one easy-to-navigate site. Now you'll no longer have to kick tires on lemon lots, scroll though endless Facebook pages or search countless individual dealership sites to find your next vehicle:</p>
			<p class="text-center"><%= link_to "Let Polestar Guide You!", cars_path, class: 'btn btn-lg btn-primary' %></p>

			<h4>Featured Vehicles:</h4>
			<div class="row">
				<!-- Show four most recent uploads -->
				<% if @car1 %>
					<% if @car1.image %>
						<div class="col-md-3 text-center">
							<%= link_to @car1 do %>
								<%= image_tag @car1.image.url(:medium), class: 'homepage-car-img' %>
							<% end %>
						</div>
					<% end %>
				<% end %>

				<% if @car2 %>
					<% if @car2.image %>
						<div class="col-md-3 text-center">
							<%= link_to @car2 do %>
								<%= image_tag @car2.image.url(:medium), class: 'homepage-car-img' %>
							<% end %>
						</div>
					<% end %>
				<% end %>

				<% if @car3 %>
					<% if @car3.image %>
						<div class="col-md-3 text-center">
							<%= link_to @car3 do %>
								<%= image_tag @car3.image.url(:medium), class: 'homepage-car-img' %>
							<% end %>
						</div>
					<% end %>
				<% end %>

				<% if @car4 %>
					<% if @car4.image %>
						<div class="col-md-3 text-center">
							<%= link_to @car4 do %>
								<%= image_tag @car4.image.url(:medium), class: 'homepage-car-img' %>
							<% end %>
						</div>
					<% end %>
				<% end %>

			</div>
			<br />
		</div>
	</div>
</div>
